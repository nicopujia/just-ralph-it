repos:
  - repo: local
    hooks:
      - id: lint
        name: lint
        entry: make lint
        language: system
        types: [python]
        pass_filenames: false

      - id: format
        name: format
        entry: make format
        language: system
        types: [python]
        pass_filenames: false

      - id: typecheck
        name: typecheck
        entry: make typecheck
        language: system
        pass_filenames: false
        stages: [pre-push]

      - id: test
        name: test
        entry: make test
        language: system
        pass_filenames: false
        stages: [pre-push]
