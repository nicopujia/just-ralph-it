default_install_hook_types: [pre-commit, pre-push]

repos:
  - repo: local
    hooks:
      - id: validate-fast
        name: fast validation
        entry: ./scripts/validate.py --mode fast
        language: system
        pass_filenames: false
        stages: [pre-commit]
      - id: validate-full
        name: smoke validation
        entry: ./scripts/validate.py --mode smoke
        language: system
        pass_filenames: false
        stages: [pre-push]
