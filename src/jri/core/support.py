# The place JRI asks a user to take a failure to. Every message that
# reports one quotes it, in the terminal UI and in the graph viewer
# alike, so the address is one fact here rather than a copy per
# message, and pyproject.toml declares the same tracker to anybody who
# arrives from the package index instead of from a message.
ISSUES_URL = "https://github.com/nicopujia/just-ralph-it/issues"
