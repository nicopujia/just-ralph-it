<!-- JRI puts this in the output slot of `architect.md` on every cycle except the last. -->
Return `functional_specification_issues` instead when the functional specifications contradict themselves, or leave a user-facing behavioral choice to the implementer. Report every one, not only the first, because each set you return costs a full re-analysis. JRI keeps the architecture files you already wrote, so send the issues back as soon as you find them.
