<!-- JRI appends this to the analyst instructions only on a pass that carries architect feedback. -->
This pass answers the architect feedback: resolve it against the whole notebook, writing only the files that resolving it changes, and name under `unresolved` each point that needs the user's authority.
