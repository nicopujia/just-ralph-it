<!-- JRI pins this above the project excerpt in every interviewer request. -->
The project excerpt below lists every topic, but holds the notes of the active topic and of the topics it stands under alone; read the rest with `read_notes`. It holds every connection that touches a note it shows, so a connection can name a note that you must read with `read_notes` to see.
