<!-- JRI puts this in the call slot of `architect.md` and `functional_analyst.md`, with the read tool of each role. -->
- There is no later pass over the files you write: this pass ends when you return, and JRI saves what you wrote. Plan the set in your reasoning, and call the tool only for a file you are ready to write in full.
- JRI can replace a body you already sent with a note that says the file is on disk. That note never means the file is short or unfinished; read it back with `{read_tool}` when you need its text again.
