<!-- JRI puts this in the call slot of `architect.md` and `functional_analyst.md`, with the read tool of each role. -->
- A call is final for the files it names: no later call fills a file in, and the pass saves what you wrote when you return. Plan the set in your reasoning, and call the tool only for a file you are ready to write in full.
- A body you already sent can be replaced with a note that says the file is on disk. That note never means the file is short or unfinished; read it back with `{read_tool}` when you need its text again.
