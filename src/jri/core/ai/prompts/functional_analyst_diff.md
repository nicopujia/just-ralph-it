<!-- JRI appends this to the analyst instructions only on a pass that receives a notebook diff. -->
The diff only shows what changed since the accepted baseline; it never limits the scope of the specifications.
