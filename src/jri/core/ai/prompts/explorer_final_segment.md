<!-- JRI puts this in the final slot of `explorer_segment.md`, on the last segment an exploration can run. -->
This is the last segment of this exploration. No segment follows it, so report what you have at the end of it and leave `remaining` empty.
