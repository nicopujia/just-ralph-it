title: Project Management
priority: 0
created: '2026-03-21'
description: 'CRUD operations for projects: creation (with GitHub repo), dashboard
  listing, deletion.'
acceptance_criteria:
- Users can create projects (which creates a GitHub repo via ralphpujia, adds user
  as collaborator, initializes bd+git), view them on a dashboard, and delete them
  (optionally deleting the GitHub repo).
