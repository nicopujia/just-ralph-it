0a. Study `.jri/specs/*` with up to 250 parallel Codex subagents to learn the application specifications.
0b. Study @.jri/IMPLEMENTATION_PLAN.md (if present) to understand the plan so far.
0c. If present, study `src/lib/*` with up to 250 parallel Codex subagents to understand shared utilities & components.
0d. For reference, the application source code is in `src/*`, if present.

1. Study @.jri/IMPLEMENTATION_PLAN.md (if present; it may be incorrect) and use up to 500 Codex subagents to study existing source code in `src/*` and compare it against `.jri/specs/*`. Use a GPT-5.5-xhigh subagent to analyze findings, prioritize tasks, and create/update @.jri/IMPLEMENTATION_PLAN.md as a bullet point list sorted in priority of items yet to be implemented. Ultrathink. Consider searching for TODO, minimal implementations, placeholders, skipped/flaky tests, and inconsistent patterns. Study @.jri/IMPLEMENTATION_PLAN.md to determine starting point for research and keep it up to date with items considered complete/incomplete using subagents.

IMPORTANT: Plan only. Do NOT implement anything. Do NOT assume functionality is missing; confirm with code search first. Treat `src/lib` as the project's standard library for shared utilities and components if it exists. Prefer consolidated, idiomatic implementations there over ad-hoc copies.

ULTIMATE GOAL: We want to achieve Just Ralph It, the proper tool around the Ralph Wiggum technique (relevant external links available at @README.md) that makes it stupidly seamless to use the power of the technique without having to learn it or set things up, the current scope being to build the core and the CLI client. Consider missing elements and plan accordingly. If an element is missing, search first to confirm it doesn't exist, then if needed author the specification at .jri/specs/FILENAME.md. If specs contradict each other or leave behavior ambiguous enough that implementation could produce materially different user-facing results, update the specs first using a GPT-5.5-xhigh subagent and treat that as part of planning, not building. If you create or update a spec then document the plan to implement it in @.jri/IMPLEMENTATION_PLAN.md using a subagent.
