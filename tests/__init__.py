"""Test suite for JRI.

- Fake, mock, dummy, stub, and spy objects must go under `doubles/`
- Integration tests must follow a black box approach (i.e. assume
the implementation is unknown and focus only on the behavior).
- All tests must be idempotent and leave no traces once they
have finished.
"""
