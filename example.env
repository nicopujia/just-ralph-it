# Canonical git remote URL for the JRI self-hosting repo. When the current repo's
# remote matches this value, JRI refreshes Ralph's runtime between iterations.
REMOTE_URL=https://github.com/your-org/justralph.it
