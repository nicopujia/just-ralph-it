# JRI does not load this file automatically.
# Source it yourself or export the variables in your shell.

# Optional fallback model override for `jri start` Ralph runs.
# Prefer `jri start --model ...` for per-run selection.
JRI_OPENCODE_MODEL=opencode/qwen3.6-plus-free

# Test-only variables used by the live OpenCode test suite.
# JRI_RUN_LIVE_OPENCODE=1
# JRI_TEST_MODEL=opencode/qwen3.6-plus-free
